import 'package:flutter/material.dart';
class Hot_DealesScreen extends StatelessWidget {
  const Hot_DealesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Center(
        child: Text('Hot Deales Screen',
          style: TextStyle(
            fontWeight: FontWeight.bold,
            fontSize: 20,
          ),),
      );
  }
}
